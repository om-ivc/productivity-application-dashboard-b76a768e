# ============================================
# DATABASE CONFIGURATION
# ============================================
# Choose ONE of the following database configurations:

# OPTION 1: Supabase (for client apps)
# Get these values from https://supabase.com/dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ⚠️ SECURITY: This key bypasses Row Level Security (RLS)
# NEVER expose this to the browser or commit it to version control
# Get it from: Supabase Dashboard > Project Settings > API > service_role key
# Required for server-side CRUD operations in API routes and Server Components
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OPTION 2: AWS Aurora PostgreSQL (for base apps)
# PostgreSQL connection string for AWS Aurora
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@your-aurora-cluster-endpoint:5432/database
POSTGRES_URL=postgresql://user:password@your-aurora-cluster-endpoint:5432/database

# Aurora connection details (alternative to connection string)
# DATABASE_HOST=your-aurora-cluster-endpoint.region.rds.amazonaws.com
# DATABASE_PORT=5432
# DATABASE_NAME=your-database-name
# DATABASE_USER=your-database-user
# DATABASE_PASSWORD=your-database-password
# DATABASE_SSL=true

# ============================================
# OPTIONAL: Additional Configuration
# ============================================

# Only needed if using MongoDB
# MONGODB_URI=mongodb://localhost:27017/your-database

# Only needed if implementing custom JWT authentication
# JWT_SECRET=your-secret-key

# App Configuration (optional)
NEXT_PUBLIC_APP_NAME=Your App Name

# Production URL for Swagger API documentation
# This should be your production deployment URL (e.g., https://your-app.vercel.app)
NEXT_PUBLIC_APP_URL=https://your-production-url.com

